export function calculate(nextValue, totalValue, button) {
  console.log(`nextValue: ${nextValue}`);
  console.log(`totalValue: ${totalValue}`);
  console.log(`button: ${button}`);
  return { nextValue: nextValue, totalValue: totalValue };
}
